/// A favorite color.
enum Color {
    Red,
    Blue,
}

/// A piece of mail.
struct Mail {
    /// The destination address.
    address: String,
}

fn add(a: i32, b: i32) -> i32 {
    a + b
}

// use cmd in terminal: cargo doc --open
fn main() {}
